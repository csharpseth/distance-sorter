export enum DrawType {
    DAISY_CHAIN = 'Normal',
    TO_ORIGIN = 'Origin'
}

export enum SortType {
    INSERSTION_SORT = 'Insertion'
}

export enum AcknowledgementType {
    YES = 'Yes',
    NO = 'No'
}